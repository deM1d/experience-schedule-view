package com.idemidov.experiencescheduleview

import android.graphics.Color

class ExperiencesScheduleColorPalette {

    var backgroundColor: Int = Color.WHITE
    var yearColor: Int = Color.BLACK
    var monthColor: Int = Color.BLACK
    var daysColor: Int = Color.BLACK

    var slotBackgroundAvailableColor: Int = Color.GREEN
    var slotBackgroundBookedColor: Int = Color.RED
    var slotBackgroundInactiveColor: Int = Color.GRAY

    var slotTextAvailableColor: Int = Color.WHITE
    var slotTextBookedColor: Int = Color.BLACK
    var slotTextInactiveColor: Int = Color.DKGRAY
}